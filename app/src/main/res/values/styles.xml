<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="Widget.SearchView" parent="Widget.AppCompat.SearchView">
        <!-- Text color -->
        <item name="android:textColorPrimary">@color/colorDefaultText</item>
        <!-- Hint (search icon) color -->
        <item name="android:textColorSecondary">@color/colorDefaultText</item>
        <!-- Icon color -->
        <item name="android:tint">@color/colorDefaultText</item>
    </style>

</resources>
